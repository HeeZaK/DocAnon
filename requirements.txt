# Interface utilisateur
streamlit>=1.30.0

# Traitement PDF et images scannées
pdf2image
pytesseract
Pillow

# Fichiers Word (.docx)
python-docx

# Traitement PDF natifs
PyMuPDF

# Extraction OCR ou brute (attention : textract peut avoir des dépendances C lourdes)
textract

# Traitement du langage naturel
spacy==3.7.2           # Version stable compatible avec fr_core_news_sm-3.7.0
scikit-learn

# Modèles avancés (transformers, torch) — pour résumé/classification
transformers
torch==2.1.2           # Évite les erreurs de compatibilité avec numpy

# Fix pour erreurs numpy/torch
numpy==1.24.4          # Compatible avec torch et évite le dtype size mismatch

# Optionnel : télécharger fr_core_news_sm automatiquement
# Installer séparément dans le code ou dans un setup.sh
